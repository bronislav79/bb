{
  "name": "bb-whisk-batch",
  "version": "1.0.0",
  "description": "Batch parallel image generation via Google Whisk API",
  "type": "module",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "generate": "node --loader ts-node/esm src/index.ts"
  },
  "dependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
